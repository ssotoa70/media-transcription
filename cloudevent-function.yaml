# Test CloudEvent for Function trigger
# Simulates a function-to-function invocation with an explicit file target.
#
# Usage:
#   vast functions invoke -e cloudevent-function.yaml -u http://localhost:8080
#
# The calling function specifies the exact bucket/key to transcribe.

specversion: "1.0"
type: "vastdata.com:Function"
source: "vastdata.com:orchestrator-function"
id: "c3d4e5f6-a7b8-4c5d-9e8f-2a3b4c5d6e7f"
time: "2026-04-06T12:00:00Z"
datacontenttype: "application/json"
data:
  bucket: "media-assets"
  key: "uploads/interview.mp4"
# Note: Function events do NOT require subject field
# Note: Function events have no subtype
